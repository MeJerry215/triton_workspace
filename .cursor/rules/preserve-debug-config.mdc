---
description: 不要删除用户主动添加的调试/诊断/DUMP 配置代码
alwaysApply: true
---

# 不要删除用户的调试/诊断配置

当用户显式在代码中放置了调试/诊断相关的配置（如 `pass_configs`、`TL_ENABLE_DUMP_IR`、dump 目录、profiler 设置等）时：

1. **不要删除它们** — 即使用户要求"移除无效的地方"或"清理代码"，调试配置不属于可移除的"无效代码"，除非用户明确指名要删除某个具体的配置项。
2. **识别"用户迭代"的信号** — 如果用户围绕同一块配置反复修改（例如从字符串 key 改为 enum 成员、调整参数等），说明这块代码是用户**主动维护**的，绝不要自作主张删除。
3. **先确认再动手** — 如果不确定某段代码是否属于"无效"，先问用户，而不是直接删掉。
