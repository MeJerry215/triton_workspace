---
description: 当用户指出 markdown 文档中的错误时直接修复
globs: **/markdowns/*.md
alwaysApply: false
---

# 自动修复 markdown 文档错误

当用户指出 `markdowns/` 目录下的 markdown 文档中存在错误（如表格数值、公式、逻辑描述、代码示例等不正确）时：

1. **直接修复** — 确认用户指出的问题确实存在后，立即修改文档，不需要询问用户是否要改
2. **连带修复** — 如果错误在一个位置引发了其他内容的不一致（如表格改了一个值后，下游的步骤说明、最终结果等也引用该值），一并修正所有受影响的地方
3. **只修复明确指出的错误** — 不要在修复时擅自改动文档的其他部分
