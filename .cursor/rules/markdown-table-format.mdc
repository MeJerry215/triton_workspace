---
description: Markdown 表格格式规范 — 分隔线列数必须与表头/数据行一致
globs: **/*.md
alwaysApply: false
---

# Markdown 表格格式规范

## 表格分隔线的列数必须与数据行一致

这是最常见的 markdown 表格显示问题：分隔线行（第二行）的 `---` 组数必须与表头行的列数相同。

### 错误示例（显示会错乱）

```markdown
| | `@tilelang.jit` | `@T.prim_func` |
|---|---|---|---|
| **角色** | 完整的编译管线 | Python AST 转换器 |
```

分隔线写了 4 个 `---` 组，但表头和数据行只有 3 列。

### 正确示例

```markdown
| | `@tilelang.jit` | `@T.prim_func` |
|---|---|---|
| **角色** | 完整的编译管线 | Python AST 转换器 |
```

分隔线 3 个 `---` 组，与表头 3 列一致。

## 检查方法

- 数表头行（第一行）的 `|` 分隔符数量，减 1 得到列数
- 分隔线行（第二行）的 `---` 组数必须等于列数
- 所有数据行的 `|` 分隔符数量也必须一致

## 常见问题

1. `||` 开头表示第一列为空（双竖线之间无内容）
2. `| |` 开头表示第一列内容为一个空格
3. 每行首尾的 `|` 是表格边框，不计入列内容

## 避坑：区分 Read 工具的输出格式与文件内容

当使用 `Read` 工具查看 markdown 文件时，每一行输出格式为：

```
LINE_NUMBER|LINE_CONTENT
```

`|` 是行号和文件内容之间的**工具分隔符**，不是文件中的字符。

### 容易混淆的场景

假设 Read 工具显示：

```
  1746|| Layout 类型 | 输入维 | 含义 | 来源 |
  1747||---|---|---|---|---|
```

这里 `  1746` 后的第一个 `|` 是工具分隔符，真正的文件内容从第二个 `|` 开始。即：

- 表头实际内容：`| Layout 类型 | 输入维 | 含义 | 来源 |`（4 列）
- 分隔线实际内容：`|---|---|---|---|---|`（5 个 `---` 组 = 5 列）❌

**分隔线比表头多了一列**，导致表格无法正确渲染。

### 正确的计数方法

**不要数表头的 `|` 符号数量**（容易受行号前缀干扰），直接数分隔线行有几个 `---` 组，那才是真正的列数：

| 分隔线 | `---` 组数 | 列数 |
|--------|-----------|------|
| `|---|---|---|` | 3 | 3 列 |
| `|---|---|---|---|` | 4 | 4 列 |
| `|---|---|---|---|---|` | 5 | 5 列 |

### 修复方法

把多出来的 `|---|` 组删掉，使分隔线组数 = 表头列数：

```markdown
| Layout 类型 | 输入维 | 含义 | 来源 |    ← 4 列
|---|---|---|---|                          ← 4 个 --- 组 ✓
```

### 错误示例对比

```markdown
| A | B | C |
|---|---|---|---|    ← 4 个组，但只有 3 列 ❌
```

```markdown
| A | B | C |
|---|---|---|      ← 3 个组，3 列 ✓
```
